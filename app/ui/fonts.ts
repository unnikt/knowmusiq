import { Inter, Lusitana, Anek_Malayalam } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
});
export const malayalam = Anek_Malayalam({
    weight: ['400', '700'],
    subsets: ['latin'],
});